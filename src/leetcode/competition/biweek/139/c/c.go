package main

/**
给你一个整数数组 nums 和一个 正 整数 k 。

定义长度为 2 * x 的序列 seq 的 值 为：

(seq[0] OR seq[1] OR ... OR seq[x - 1]) XOR (seq[x] OR seq[x + 1] OR ... OR seq[2 * x - 1]).
请你求出 nums 中所有长度为 2 * k 的
子序列
 的 最大值 。



示例 1：

输入：nums = [2,6,7], k = 1

输出：5

解释：

子序列 [2, 7] 的值最大，为 2 XOR 7 = 5 。


*/

func maxValue(nums []int, k int) int {

	//n := len(nums)
	//f1 := make([][]map[int]bool, n)
	//f2 := make([][][]int, n)
	//
	//leftMin := make(map[int]int)
	//
	//rightMax := make(map[int]int)
	//for i := range f1 {
	//	f1[i] = make([][]int, k+1)
	//
	//}
	//
	//for i := 0; i < n; i++ {
	//	f1[i][0][nums[i]] = true
	//	for j := 0; j < i; j++ {
	//		for t := 0; t < k; t++ {
	//			for c:=
	//		}
	//	}
	//
	//}
	//
	//for i := range f2 {
	//	f2[i] = make([]int, k+1)
	//}
	//
	//for i := 0; i < n; i++ {
	//
	//}

	return 1

}
